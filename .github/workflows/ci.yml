name: CI - Test & Lint

on:
  push:
    branches-ignore: [main]          # run on feature branches, not main
  pull_request:
    branches: [main]

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
          # Install test & lint tools if not in requirements.txt
          pip install pytest pytest-cov ruff

      - name: Lint with Ruff
        run: |
          ruff check . --output-format=github
          # Optional: fail on lint errors
          # ruff check . --exit-non-zero-on-fix

      # Future steps: test coverage, security scan, dependency check, etc.
      #- name: Run tests with coverage
      #  run: |
      #    pytest --cov=. --cov-report=xml --cov-report=term-missing

      #- name: Upload coverage to Codecov
      #  uses: codecov/codecov-action@v4
      #  with:
      #    token: ${{ secrets.CODECOV_TOKEN }}  # optional: add secret if you want uploads
      #    fail_ci_if_error: true
      #    verbose: true

      # - name: Security scan (Trivy or similar)
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     scan-type: 'fs'
      #     format: 'table'
      #     exit-code: '1'  # fail on critical vulnerabilities
